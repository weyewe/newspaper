<% if current_user && current_user.has_role?(:writer) %>
  <%= render :partial => "writer_header" %>
<% end %>

<% if current_user && current_user.has_role?(:editor) %>
  <%= render :partial => "editor_header" %>
<% end %>
