<div class="mainStory fleft">
  <h1>
    <%= link_to @main_story.title , story_public_url(@main_story)%>
  </h1>
  

  
  <%= link_to story_public_url(@main_story) do %>
    <%= image_tag StoryImage.find(:first, :conditions => {
      :image_type => "homepage",
      :story_id => @main_story.id
    }).url %>
  <% end %>

  <div class="dek">Mau tahu lebih lengkapnya? Click <b>
    <%= link_to "Continue »", story_public_url(@main_story)%>
    </b></div>
  <div class="byLine"><%= @main_story.user.profile.name %></div>
</div>
